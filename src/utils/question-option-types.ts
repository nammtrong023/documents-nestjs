import { QuestionTypeEnum } from 'src/enum/app.enum';

export function QuestionOptionTypes(type: string) {
  switch (type) {
    case (type = QuestionTypeEnum.SingleChoice):
      options: {
      }
      break;

    default:
      break;
  }
}
